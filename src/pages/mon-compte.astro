---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Mon Compte — MARYAN"
  description="Votre point d’entrée MARYAN : agir, comprendre et reprendre rapidement.">

  <main class="dashboard-page">
    <div class="container dashboard-layout">
      <aside class="dashboard-sidebar">
        <div class="dashboard-account card-v3">
          <span class="label-v3">Compte</span>
          <h2 id="profileName">Chargement…</h2>
          <p id="profileMeta" class="dashboard-account-meta"></p>

          <div class="dashboard-account-stats">
            <div class="dashboard-account-stat">
              <span>Forfait</span>
              <strong id="planVal">–</strong>
            </div>
            <div class="dashboard-account-stat">
              <span>Email</span>
              <strong id="userEmail">–</strong>
            </div>
          </div>

          <div class="dashboard-account-actions">
            <a href="/copilote?mode=document" class="btn-secondary-v3">Décrypter un document</a>
            <a href="/tests" class="btn-secondary-v3">Voir les tests</a>
            <button id="exportDataBtn" class="btn-secondary-v3" type="button">Télécharger mes données</button>
            <button id="logoutBtn" class="dashboard-logout-btn" type="button">Se déconnecter</button>
          </div>

          <section class="dashboard-profile-form-wrap">
            <h3>Compléter mon profil</h3>
            <p>Votre espace de travail MARYAN devient plus pertinent quand ces informations sont renseignées.</p>

            <form id="profileForm" class="dashboard-profile-form" novalidate>
              <div class="dashboard-profile-grid">
                <label>
                  <span>Prénom</span>
                  <input id="profileFirstName" type="text" autocomplete="given-name" placeholder="Prénom" />
                </label>
                <label>
                  <span>Nom</span>
                  <input id="profileLastName" type="text" autocomplete="family-name" placeholder="Nom" />
                </label>
              </div>

              <label>
                <span>Rôle dans la collectivité</span>
                <input id="profileRole" type="text" placeholder="Ex : Adjoint·e au maire" />
              </label>

              <label>
                <span>Collectivité (commune / territoire)</span>
                <input id="profileCommune" type="text" placeholder="Ex : Ville de Nantes" />
              </label>

              <label>
                <span>Nature de collectivité</span>
                <select id="profileCollectivityType">
                  <option value="">— Choisir —</option>
                  <option value="mairie">Mairie / Commune</option>
                  <option value="epci">EPCI / Intercommunalité</option>
                  <option value="departement">Département</option>
                  <option value="region">Région</option>
                  <option value="metropole">Métropole</option>
                  <option value="autre">Autre collectivité locale</option>
                </select>
              </label>

              <label>
                <span>Taille de collectivité</span>
                <select id="profileCollectivitySize">
                  <option value="">— Choisir —</option>
                  <option value="moins_3500">Moins de 3 500 habitants</option>
                  <option value="3500_10000">3 500 à 10 000 habitants</option>
                  <option value="10000_50000">10 000 à 50 000 habitants</option>
                  <option value="50000_100000">50 000 à 100 000 habitants</option>
                  <option value="plus_100000">Plus de 100 000 habitants</option>
                </select>
              </label>

              <label>
                <span>Affiliation politique (facultatif)</span>
                <input id="profilePoliticalAffiliation" type="text" placeholder="Nom du parti, si vous souhaitez le préciser" />
              </label>

              <label class="dashboard-checkbox-row">
                <input id="profileHasOtherJob" type="checkbox" />
                <span>J’exerce aussi une activité professionnelle en parallèle</span>
              </label>

              <label id="profileOtherJobWrap" style="display:none;">
                <span>Activité professionnelle (facultatif)</span>
                <input id="profileOtherJobTitle" type="text" placeholder="Ex : Enseignante, avocat, cadre territorial…" />
              </label>

              <div id="profileFormFeedback" class="dashboard-profile-feedback" style="display:none;"></div>
              <button id="profileSaveBtn" type="submit" class="btn-primary-v3">Enregistrer mon profil</button>
            </form>
          </section>
        </div>
      </aside>

      <div class="dashboard-main">
        <div id="paymentBanner" class="dashboard-banner" style="display:none;">
          <strong>Abonnement activé</strong>
          <p>Votre accès MARYAN Plus est maintenant disponible.</p>
        </div>

        <section id="subscriptionBlock" class="dashboard-banner dashboard-banner-upgrade" style="display:none;">
          <strong>Passez en mode MARYAN Plus</strong>
          <p>Activez l’offre pour débloquer un usage complet du copilote, des fiches expertes et l’analyseur.</p>
          <a href="/offres#offre-individuelle" class="btn-primary-v3">Voir les offres</a>
        </section>

        <section class="dashboard-hero card-v3">
          <span class="label-v3">Mon espace</span>
          <h1 id="helloTitle">Bonjour élu·e,</h1>
          <p id="helloLead" class="dashboard-hero-copy">Chargement…</p>
        </section>

        <section class="dashboard-block">
          <div class="dashboard-block-head">
            <div>
              <h2>Que souhaitez-vous faire ?</h2>
            </div>
          </div>

          <div class="dashboard-action-grid">
            <a href="/diagnostic" class="dashboard-action-card" data-intent="comprendre">
              <span>Comprendre une situation</span>
            </a>
            <a href="/copilote" class="dashboard-action-card" data-intent="decision">
              <span>Prendre une décision</span>
            </a>
            <a href="/copilote" class="dashboard-action-card" data-intent="sensible">
              <span>Gérer une situation sensible</span>
            </a>
            <a href="/copilote" class="dashboard-action-card" data-intent="reponse">
              <span>Rédiger une réponse</span>
            </a>
          </div>

          <div class="dashboard-support-row">
            <a href="/copilote?mode=document" class="dashboard-support-link">Décrypter un document officiel →</a>
          </div>
        </section>

        <section class="dashboard-block">
          <div class="dashboard-block-head">
            <div>
              <h2>Le Radar de MARYAN</h2>
              <p>À venir.</p>
            </div>
            <a href="/radar" class="btn-link">Voir la page →</a>
          </div>

          <div id="dashboardRadarList" class="dashboard-radar-list"></div>
        </section>

        <section class="dashboard-block">
          <div class="dashboard-block-head">
            <div>
              <h2>Vos sujets du moment</h2>
              <p id="topicsLead">Des points d’appui choisis à partir de votre diagnostic.</p>
            </div>
            <a href="/ressources" class="btn-link">Explorer les ressources →</a>
          </div>

          <div id="topicsGrid" class="dashboard-topics-grid"></div>
        </section>

        <section class="dashboard-block">
          <div class="dashboard-block-head">
            <div>
              <h2>Reprendre</h2>
            </div>
          </div>

          <div id="resumeCard" class="dashboard-resume-card card-v3"></div>
        </section>

        <!-- ══ MES DOCUMENTS ══ -->
        <section class="dashboard-block" id="docs-section">
          <div class="dashboard-block-head">
            <div>
              <h2>Mes documents</h2>
              <p>Vos comptes rendus et analyses sauvegardés.</p>
            </div>
            <a href="/copilote" class="btn-primary-v3 dashboard-head-cta">Créer un document</a>
          </div>

          <!-- Dossiers -->
          <div class="docs-folders-bar" id="docsFoldersBar">
            <div id="docsFoldersList" class="docs-folders-list"></div>
            <button id="docsNewFolderBtn" class="docs-new-folder-btn" type="button">+ Nouveau dossier</button>
          </div>
          <div id="docsNewFolderForm" class="docs-new-folder-form" style="display:none">
            <input id="docsNewFolderInput" type="text" class="docs-folder-input" placeholder="Nom du dossier" maxlength="60" />
            <button id="docsNewFolderSave" class="btn-mode-sm-primary" type="button">Créer</button>
            <button id="docsNewFolderCancel" class="btn-mode-sm-outline" type="button">Annuler</button>
          </div>

          <!-- Liste des documents -->
          <div id="docsListContainer" class="docs-list-container">
            <div id="docsLoading" class="docs-loading">Chargement…</div>
            <div id="docsEmpty" class="docs-empty" style="display:none">
              <p>Aucun document dans ce dossier.</p>
              <a href="/copilote" class="btn-primary-v3">Créer un compte rendu ou analyser un document</a>
            </div>
            <div id="docsList" class="docs-list" style="display:none"></div>
          </div>

          <!-- Panneau de lecture -->
          <div id="docsReadPanel" class="docs-read-panel" style="display:none">
            <div class="docs-read-header">
              <button id="docsClosePanel" class="docs-close-panel-btn" type="button">← Retour à la liste</button>
              <button id="docsPanelCopyBtn" class="btn-mode-sm-secondary" type="button">Copier</button>
            </div>
            <div class="docs-read-meta">
              <span id="docsPanelType" class="docs-type-badge"></span>
              <span id="docsPanelDate" class="docs-date"></span>
            </div>
            <h3 id="docsPanelTitle" class="docs-read-title"></h3>
            <div id="docsPanelContent" class="docs-read-content"></div>
          </div>
        </section>
      </div>
    </div>
  </main>

  <!-- Confirm modal -->
  <div id="docsDeleteModal" class="docs-modal-overlay" style="display:none" role="dialog" aria-modal="true">
    <div class="docs-modal">
      <p class="docs-modal-title">Supprimer ce document ?</p>
      <p class="docs-modal-text">Cette action est irréversible.</p>
      <div class="docs-modal-actions">
        <button id="docsDeleteConfirm" class="btn-mode-sm-danger" type="button">Supprimer</button>
        <button id="docsDeleteCancel" class="btn-mode-sm-outline" type="button">Annuler</button>
      </div>
    </div>
  </div>

</BaseLayout>

<script>
  import { createClient } from '@supabase/supabase-js';
  import { getRecommendedResources, maryanResources } from '../data/resources';
  import {
    buildDiagnosticStateFromProfileRow,
    buildMaryanProfileFromDiagnosticState
  } from '../lib/user-personalization';

  const SUPABASE_URL = import.meta.env.PUBLIC_SUPABASE_URL as string;
  const SUPABASE_KEY = import.meta.env.PUBLIC_SUPABASE_ANON_KEY as string;

  const ANGLES_BY_PROFILE = {
    mandat_recent: ['compréhension', 'repères', 'décision'],
    arbitrage: ['compréhension', 'arbitrage', 'décision'],
    isolement: ['clarification', 'appuis', 'sécurisation'],
    surcharge: ['priorisation', 'sécurisation', 'décision'],
    exposition: ['vigilance', 'parole', 'sécurisation'],
    tension_relationnelle: ['relation', 'cadre', 'décision'],
    besoin_methode: ['compréhension', 'méthode', 'décision'],
    prise_de_parole: ['parole', 'vigilance', 'décision'],
    gouvernance: ['circuits', 'sécurisation', 'décision']
  } as const;

  const GENERIC_RESOURCES = [
    'qui-decide-quoi-dans-la-collectivite',
    'le-conseil-municipal-a-quoi-il-sert-vraiment',
    'prendre-la-parole-en-conseil',
    'comment-se-prend-une-decision-dans-une-commune'
  ];

  const ROLE_LABELS: Record<string, string> = {
    maire: 'Maire',
    adjoint: 'Adjoint·e au maire',
    conseiller_maj: 'Conseiller·e de la majorité',
    conseiller_opp: 'Conseiller·e d’opposition',
    interco: 'Président·e / VP d’intercommunalité'
  };

  function escapeHtml(value: unknown): string {
    return String(value ?? '')
      .replaceAll('&', '&amp;')
      .replaceAll('<', '&lt;')
      .replaceAll('>', '&gt;')
      .replaceAll('"', '&quot;')
      .replaceAll("'", '&#39;');
  }

  function formatShortDate(iso: string): string {
    try {
      return new Date(iso).toLocaleDateString('fr-FR', { day: 'numeric', month: 'short' });
    } catch {
      return '';
    }
  }

  function normalizeText(value: unknown): string {
    return typeof value === 'string' ? value.trim() : '';
  }

  function getRoleLabel(value: unknown): string {
    const key = normalizeText(value);
    if (!key) return '';
    return ROLE_LABELS[key] || key;
  }

  function sessionLabel(type: string): string {
    const labels: Record<string, string> = {
      copilote: 'Copilote',
      cr_reunion: 'Compte rendu',
      analyse: 'Analyse'
    };
    return labels[type] || 'Session';
  }

  function sessionHref(session: { id: string; session_type: string }): string {
    if (session.session_type === 'analyse') return '/copilote?mode=document';
    return `/copilote?session_id=${session.id}`;
  }

  if (!SUPABASE_URL || !SUPABASE_KEY) {
    window.location.href = '/login';
  } else {
    const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);

    if (window.location.search.includes('checkout=success')) {
      const paymentBanner = document.getElementById('paymentBanner');
      if (paymentBanner) paymentBanner.style.display = 'block';
      window.history.replaceState({}, '', '/mon-compte');
    }

    const { data: { session } } = await supabase.auth.getSession();
    if (!session) {
      window.location.href = '/login';
    } else {
      const user = session.user;
      const token = session.access_token;

      let profileContext = null;
      try {
        const response = await fetch('/api/profile-context', {
          headers: { Authorization: `Bearer ${token}` }
        });
        if (response.ok) {
          profileContext = await response.json();
        }
      } catch {
        // noop
      }

      const profile = profileContext?.profile || (await supabase
        .from('profiles')
        .select('first_name, commune, role, parti_label, diagnostic_key, diagnostic_label, diagnostic_completed, last_diagnostic_summary, profile_context, profile_context_updated_at, plan')
        .eq('id', user.id)
        .single()).data;

      const personalInfo = profileContext?.personalInfo && typeof profileContext.personalInfo === 'object'
        ? profileContext.personalInfo as Record<string, unknown>
        : {};

      const firstName = profile?.first_name || '';
      const diagnosticState = profileContext?.diagnosticState || buildDiagnosticStateFromProfileRow(profile);
      const role = getRoleLabel(profile?.role || diagnosticState?.role) || 'élu·e local·e';
      const commune = profile?.commune || '';
      const plan = profile?.plan || 'gratuit';
      const isSubscriber = plan === 'plus' || plan === 'admin';
      const lastName = normalizeText(personalInfo.last_name);
      const fullName = [firstName, lastName].filter(Boolean).join(' ');

      document.getElementById('profileName')!.textContent = fullName || firstName || user.email?.split('@')[0] || 'Mon compte';
      document.getElementById('profileMeta')!.textContent = [role, commune].filter(Boolean).join(' — ');
      document.getElementById('userEmail')!.textContent = user.email || '';
      document.getElementById('planVal')!.textContent = plan === 'plus' ? 'MARYAN Plus' : plan === 'admin' ? 'Admin' : 'Découverte';

      const subscriptionBlock = document.getElementById('subscriptionBlock');
      if (subscriptionBlock) {
        subscriptionBlock.style.display = isSubscriber ? 'none' : 'block';
      }
      const hasDiagnostic = Boolean(diagnosticState?.diagnostic_key);
      const diagnosticSummary = diagnosticState?.summary
        || profile?.last_diagnostic_summary
        || 'Votre diagnostic est enregistré dans votre espace.';
      const diagnosticLabel = diagnosticState?.diagnostic_label || 'Diagnostic MARYAN';

      try {
        if (diagnosticState.diagnostic_key || diagnosticState.profile || diagnosticState.recommended_slugs?.length) {
          localStorage.setItem('maryan_v4_diag', JSON.stringify(diagnosticState));
        }

        const maryanProfile = profileContext?.maryanProfile || buildMaryanProfileFromDiagnosticState(diagnosticState, {
          firstName,
          commune,
          plan
        });

        if (maryanProfile) {
          localStorage.setItem('maryan_profile', JSON.stringify({
            ...maryanProfile,
            diagnosticProfile: diagnosticState.profile
          }));
        }
      } catch {
        // noop
      }

      const helloTitle = document.getElementById('helloTitle');
      if (helloTitle) {
        helloTitle.textContent = firstName ? `Bonjour ${firstName},` : 'Bonjour,';
      }

      const profileAngles = diagnosticState.profile
        ? ANGLES_BY_PROFILE[diagnosticState.profile as keyof typeof ANGLES_BY_PROFILE]
        : null;

      const angles = profileAngles
        ? profileAngles
        : ['compréhension', 'sécurisation', 'décision'];

      const helloLead = document.getElementById('helloLead');
      if (helloLead) {
        const roleSentence = [role, commune ? `à ${commune}` : 'dans votre collectivité'].join(' ');
        helloLead.textContent = `Vous êtes ${roleSentence}. MARYAN privilégie aujourd’hui : ${angles.join(', ')}.`;
      }


      const radarList = document.getElementById('dashboardRadarList');
      if (radarList) {
        radarList.innerHTML = `
          <article class="dashboard-radar-card">
            <h3>À venir.</h3>
            <p>Le Radar de MARYAN sera mis en ligne prochainement.</p>
          </article>
        `;
      }

      const topicsGrid = document.getElementById('topicsGrid');
      const topicsLead = document.getElementById('topicsLead');
      if (topicsGrid) {
        let resources = diagnosticState.profile
          ? getRecommendedResources(maryanResources, diagnosticState.profile).slice(0, 5)
          : [];

        if (!resources.length && diagnosticState.recommended_slugs?.length) {
          resources = diagnosticState.recommended_slugs
            .slice(0, 5)
            .map((slug: string) => maryanResources.find((resource) => resource.slug === slug))
            .filter(Boolean);
        }

        if (!resources.length) {
          resources = GENERIC_RESOURCES
            .map((slug) => maryanResources.find((resource) => resource.slug === slug))
            .filter(Boolean);
        }

        if (topicsLead && !diagnosticState.profile) {
          topicsLead.textContent = 'Des points d’appui personnalisés pour avancer tout de suite.';
        }

        topicsGrid.innerHTML = resources.map((resource) => `
          <a href="/ressources/${escapeHtml(resource.slug)}" class="dashboard-topic-card">
            <span class="dashboard-topic-title">${escapeHtml(resource.title)}</span>
            <span class="dashboard-topic-link">Voir la fiche →</span>
          </a>
        `).join('');
      }

      const resumeCard = document.getElementById('resumeCard');
      if (resumeCard) {
        resumeCard.innerHTML = hasDiagnostic
          ? `
          <div class="dashboard-resume-live">
            <p class="dashboard-resume-label">${escapeHtml(diagnosticLabel)}</p>
            <h3>Votre diagnostic est disponible</h3>
            <p>${escapeHtml(diagnosticSummary)}</p>
            <a href="/diagnostic" class="btn-primary-v3">Voir mon diagnostic</a>
          </div>
        `
          : `
          <div class="dashboard-resume-empty">
            <p class="dashboard-resume-label">Point d’entrée</p>
            <h3>Commencer par le diagnostic</h3>
            <p>En quelques minutes, vous obtenez une première direction claire.</p>
            <a href="/diagnostic" class="btn-primary-v3">Faire le diagnostic</a>
          </div>
        `;
      }

      const profileForm = document.getElementById('profileForm') as HTMLFormElement | null;
      const profileFirstName = document.getElementById('profileFirstName') as HTMLInputElement | null;
      const profileLastName = document.getElementById('profileLastName') as HTMLInputElement | null;
      const profileRole = document.getElementById('profileRole') as HTMLInputElement | null;
      const profileCommune = document.getElementById('profileCommune') as HTMLInputElement | null;
      const profileCollectivityType = document.getElementById('profileCollectivityType') as HTMLSelectElement | null;
      const profileCollectivitySize = document.getElementById('profileCollectivitySize') as HTMLSelectElement | null;
      const profilePoliticalAffiliation = document.getElementById('profilePoliticalAffiliation') as HTMLInputElement | null;
      const profileHasOtherJob = document.getElementById('profileHasOtherJob') as HTMLInputElement | null;
      const profileOtherJobTitle = document.getElementById('profileOtherJobTitle') as HTMLInputElement | null;
      const profileOtherJobWrap = document.getElementById('profileOtherJobWrap') as HTMLLabelElement | null;
      const profileFormFeedback = document.getElementById('profileFormFeedback') as HTMLDivElement | null;
      const profileSaveBtn = document.getElementById('profileSaveBtn') as HTMLButtonElement | null;

      if (
        profileForm && profileFirstName && profileLastName && profileRole && profileCommune &&
        profileCollectivityType && profileCollectivitySize && profilePoliticalAffiliation &&
        profileHasOtherJob && profileOtherJobTitle && profileOtherJobWrap
      ) {
        profileFirstName.value = firstName;
        profileLastName.value = lastName;
        profileRole.value = role === 'élu·e local·e' ? '' : role;
        profileCommune.value = commune;
        profileCollectivityType.value = normalizeText(personalInfo.collectivity_type);
        profileCollectivitySize.value = normalizeText(personalInfo.collectivity_size);
        profilePoliticalAffiliation.value = normalizeText(personalInfo.political_affiliation || profile?.parti_label);
        profileHasOtherJob.checked = Boolean(personalInfo.has_other_job);
        profileOtherJobTitle.value = normalizeText(personalInfo.other_job_title);
        profileOtherJobWrap.style.display = profileHasOtherJob.checked ? 'grid' : 'none';

        profileHasOtherJob.addEventListener('change', () => {
          profileOtherJobWrap.style.display = profileHasOtherJob.checked ? 'grid' : 'none';
          if (!profileHasOtherJob.checked) {
            profileOtherJobTitle.value = '';
          }
        });

        profileForm.addEventListener('submit', async (event) => {
          event.preventDefault();

          const firstNameValue = profileFirstName.value.trim();
          const lastNameValue = profileLastName.value.trim();
          const roleValue = profileRole.value.trim();
          const communeValue = profileCommune.value.trim();
          const affiliationValue = profilePoliticalAffiliation.value.trim();

          if (profileSaveBtn) profileSaveBtn.disabled = true;
          if (profileFormFeedback) {
            profileFormFeedback.style.display = 'none';
            profileFormFeedback.textContent = '';
          }

          try {
            const response = await fetch('/api/profile-context', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json',
                Authorization: `Bearer ${token}`
              },
              body: JSON.stringify({
                profile: {
                  first_name: firstNameValue || null,
                  role: roleValue || null,
                  commune: communeValue || null,
                  parti_id: null,
                  parti_label: affiliationValue || null
                },
                personalInfo: {
                  last_name: lastNameValue || null,
                  collectivity_type: profileCollectivityType.value || null,
                  collectivity_size: profileCollectivitySize.value || null,
                  political_affiliation: affiliationValue || null,
                  has_other_job: profileHasOtherJob.checked,
                  other_job_title: profileHasOtherJob.checked ? (profileOtherJobTitle.value.trim() || null) : null
                }
              })
            });

            const data = await response.json().catch(() => ({}));
            if (!response.ok) {
              throw new Error(data?.error || 'Enregistrement impossible pour le moment.');
            }

            const displayName = [firstNameValue, lastNameValue].filter(Boolean).join(' ');
            document.getElementById('profileName')!.textContent = displayName || firstNameValue || user.email?.split('@')[0] || 'Mon compte';
            document.getElementById('profileMeta')!.textContent = [getRoleLabel(roleValue || diagnosticState?.role), communeValue].filter(Boolean).join(' — ');

            if (profileFormFeedback) {
              profileFormFeedback.textContent = 'Profil mis à jour.';
              profileFormFeedback.classList.remove('dashboard-profile-feedback--error');
              profileFormFeedback.classList.add('dashboard-profile-feedback--ok');
              profileFormFeedback.style.display = 'block';
            }

            if (data?.maryanProfile) {
              localStorage.setItem('maryan_profile', JSON.stringify(data.maryanProfile));
            }
          } catch (error) {
            if (profileFormFeedback) {
              profileFormFeedback.textContent = error instanceof Error ? error.message : 'Enregistrement impossible pour le moment.';
              profileFormFeedback.classList.remove('dashboard-profile-feedback--ok');
              profileFormFeedback.classList.add('dashboard-profile-feedback--error');
              profileFormFeedback.style.display = 'block';
            }
          } finally {
            if (profileSaveBtn) profileSaveBtn.disabled = false;
          }
        });
      }

      try {
        const response = await fetch('/api/sessions', {
          headers: { Authorization: `Bearer ${token}` }
        });

        if (response.ok) {
          const { sessions } = await response.json();
          const latestSession = Array.isArray(sessions) ? sessions[0] : null;

          if (latestSession && resumeCard) {
            resumeCard.innerHTML = `
              <div class="dashboard-resume-live">
                <p class="dashboard-resume-label">${escapeHtml(sessionLabel(latestSession.session_type))}</p>
                <h3>${escapeHtml(latestSession.titre || 'Reprendre votre dernière action')}</h3>
                <p>Dernière mise à jour : ${escapeHtml(formatShortDate(latestSession.updated_at))}</p>
                <a href="${escapeHtml(sessionHref(latestSession))}" class="btn-primary-v3">Reprendre</a>
              </div>
            `;
          }
        }
      } catch {
        // noop
      }

      document.querySelectorAll('[data-intent]').forEach((link) => {
        link.addEventListener('click', () => {
          const intent = link.getAttribute('data-intent');
          if (intent) localStorage.setItem('maryan_copilot_intent', intent);
        });
      });

      // ══════════════════════════════════════════════════
      // MES DOCUMENTS — logique complète
      // ══════════════════════════════════════════════════
      {
        let currentDossier: string | null = null;
        let allDossiers: string[] = [];
        let pendingDeleteId: string | null = null;
        let currentDocContent: string = '';

        const TYPE_ICONS: Record<string, string> = {
          'compte-rendu': '🎙️',
          'document': '📄',
          'note': '💬'
        };

        function escDoc(s: unknown): string {
          return String(s ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
        }

        function fmtDate(iso: string): string {
          try { return new Date(iso).toLocaleDateString('fr-FR', { day: 'numeric', month: 'short', year: 'numeric' }); }
          catch { return ''; }
        }

        // Markdown minimal pour l'affichage dans le panneau
        function renderDocMarkdown(md: string): string {
          const esc = (s: string) => s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
          const lines = md.split('\n');
          const out: string[] = [];
          let inList = false;
          for (const raw of lines) {
            const line = raw.trimEnd();
            if (line.startsWith('## ')) {
              if (inList) { out.push('</ul>'); inList = false; }
              out.push(`<h3 class="doc-md-h2">${esc(line.slice(3))}</h3>`);
            } else if (line.startsWith('### ')) {
              if (inList) { out.push('</ul>'); inList = false; }
              out.push(`<h4 class="doc-md-h3">${esc(line.slice(4))}</h4>`);
            } else if (line.startsWith('- ') || line.startsWith('* ')) {
              if (!inList) { out.push('<ul class="doc-md-list">'); inList = true; }
              out.push(`<li>${esc(line.slice(2))}</li>`);
            } else if (line === '') {
              if (inList) { out.push('</ul>'); inList = false; }
            } else {
              if (inList) { out.push('</ul>'); inList = false; }
              out.push(`<p class="doc-md-p">${esc(line)}</p>`);
            }
          }
          if (inList) out.push('</ul>');
          return out.join('\n');
        }

        async function loadDossiers() {
          try {
            const res = await fetch('/api/documents', { headers: { Authorization: `Bearer ${token}` } });
            if (!res.ok) return;
            const data = await res.json() as { dossiers?: string[] };
            allDossiers = data.dossiers || [];
            // Dossiers par défaut toujours présents
            const defaults = ['Réunions', 'Documents', 'Notes'];
            defaults.forEach(d => { if (!allDossiers.includes(d)) allDossiers.unshift(d); });
            renderFolders();
            if (!currentDossier) {
              currentDossier = allDossiers[0] || 'Réunions';
            }
            loadDocuments(currentDossier);
          } catch { /* noop */ }
        }

        function renderFolders() {
          const list = document.getElementById('docsFoldersList')!;
          list.innerHTML = allDossiers.map(d => `
            <span class="docs-folder-chip${currentDossier === d ? ' docs-folder-btn--active' : ''}" data-folder="${escDoc(d)}">
              <button class="docs-folder-btn${currentDossier === d ? ' docs-folder-btn--active' : ''}"
                data-folder-select="${escDoc(d)}" type="button" title="Clic : sélectionner · Double-clic : renommer">
                ${escDoc(d)}
              </button>
            </span>
          `).join('');

          // Sélectionner dossier
          list.querySelectorAll('[data-folder-select]').forEach(btn => {
            btn.addEventListener('click', () => {
              currentDossier = btn.getAttribute('data-folder-select');
              renderFolders();
              loadDocuments(currentDossier!);
              closePanelIfOpen();
            });

            // Double-clic → renommer
            btn.addEventListener('dblclick', (e) => {
              e.stopPropagation();
              const folderName = btn.getAttribute('data-folder-select')!;
              const chip = btn.closest('[data-folder]') as HTMLElement;
              const input = document.createElement('input');
              input.type = 'text';
              input.value = folderName;
              input.className = 'docs-folder-rename-input';
              input.maxLength = 60;
              chip.innerHTML = '';
              chip.appendChild(input);
              input.focus();
              input.select();

              async function commitRename() {
                const newName = input.value.trim();
                if (!newName || newName === folderName) {
                  renderFolders();
                  return;
                }
                try {
                  const res = await fetch('/api/documents', {
                    method: 'PATCH',
                    headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` },
                    body: JSON.stringify({ rename_folder: { from: folderName, to: newName } })
                  });
                  if (res.ok) {
                    const idx = allDossiers.indexOf(folderName);
                    if (idx !== -1) allDossiers[idx] = newName;
                    if (currentDossier === folderName) currentDossier = newName;
                    renderFolders();
                    loadDocuments(currentDossier!);
                  } else {
                    renderFolders();
                  }
                } catch {
                  renderFolders();
                }
              }

              input.addEventListener('blur', commitRename);
              input.addEventListener('keydown', (ev: KeyboardEvent) => {
                if (ev.key === 'Enter') { input.blur(); }
                if (ev.key === 'Escape') { input.value = folderName; input.blur(); }
              });
            });
          });
        }

        async function loadDocuments(dossier: string) {
          const loadingEl = document.getElementById('docsLoading')!;
          const emptyEl = document.getElementById('docsEmpty')!;
          const listEl = document.getElementById('docsList')!;
          loadingEl.style.display = 'block';
          emptyEl.style.display = 'none';
          listEl.style.display = 'none';

          try {
            const res = await fetch(`/api/documents?dossier=${encodeURIComponent(dossier)}`, {
              headers: { Authorization: `Bearer ${token}` }
            });
            if (!res.ok) throw new Error();
            const data = await res.json() as { documents?: any[] };
            const docs = data.documents || [];
            loadingEl.style.display = 'none';
            if (!docs.length) {
              emptyEl.style.display = 'block';
            } else {
              listEl.style.display = 'block';
              renderDocsList(docs);
            }
          } catch {
            loadingEl.style.display = 'none';
            listEl.innerHTML = '<p style="color:#C62828;font-size:.9rem">Erreur de chargement.</p>';
            listEl.style.display = 'block';
          }
        }

        function renderDocsList(docs: any[]) {
          const listEl = document.getElementById('docsList')!;
          listEl.innerHTML = docs.map(doc => `
            <div class="docs-item" data-doc-id="${escDoc(doc.id)}">
              <div class="docs-item-main">
                <span class="docs-item-icon">${escDoc(TYPE_ICONS[doc.type] || '📄')}</span>
                <div class="docs-item-info">
                  <span class="docs-item-title" data-read-doc="${escDoc(doc.id)}">${escDoc(doc.titre)}</span>
                  <span class="docs-item-meta">${fmtDate(doc.created_at)}</span>
                </div>
              </div>
              <div class="docs-item-actions">
                <select class="docs-move-select" data-move-doc="${escDoc(doc.id)}" title="Déplacer vers">
                  <option value="">Déplacer…</option>
                  ${allDossiers.filter(d => d !== currentDossier).map(d => `<option value="${escDoc(d)}">${escDoc(d)}</option>`).join('')}
                </select>
                <button class="docs-delete-btn" data-delete-doc="${escDoc(doc.id)}" type="button" title="Supprimer">🗑</button>
              </div>
            </div>
          `).join('');

          // Read doc
          listEl.querySelectorAll('[data-read-doc]').forEach(el => {
            el.addEventListener('click', () => openDoc(el.getAttribute('data-read-doc')!));
          });

          // Move doc
          listEl.querySelectorAll('[data-move-doc]').forEach(sel => {
            sel.addEventListener('change', async () => {
              const docId = sel.getAttribute('data-move-doc')!;
              const newDossier = (sel as HTMLSelectElement).value;
              if (!newDossier) return;
              try {
                await fetch('/api/documents', {
                  method: 'PATCH',
                  headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` },
                  body: JSON.stringify({ id: docId, dossier: newDossier })
                });
                loadDocuments(currentDossier!);
              } catch { /* noop */ }
            });
          });

          // Delete doc
          listEl.querySelectorAll('[data-delete-doc]').forEach(btn => {
            btn.addEventListener('click', () => {
              pendingDeleteId = btn.getAttribute('data-delete-doc');
              (document.getElementById('docsDeleteModal') as HTMLElement).style.display = 'flex';
            });
          });
        }

        async function openDoc(docId: string) {
          try {
            const res = await fetch(`/api/documents?id=${docId}`, { headers: { Authorization: `Bearer ${token}` } });
            if (!res.ok) return;
            const data = await res.json() as { document?: any };
            const doc = data.document;
            if (!doc) return;
            currentDocContent = doc.contenu || '';
            document.getElementById('docsPanelTitle')!.textContent = doc.titre || 'Document';
            document.getElementById('docsPanelType')!.textContent = (TYPE_ICONS[doc.type] || '📄') + ' ' + doc.type;
            document.getElementById('docsPanelDate')!.textContent = fmtDate(doc.created_at);
            document.getElementById('docsPanelContent')!.innerHTML = renderDocMarkdown(currentDocContent);
            document.getElementById('docsListContainer')!.style.display = 'none';
            document.getElementById('docsReadPanel')!.style.display = 'block';
          } catch { /* noop */ }
        }

        function closePanelIfOpen() {
          document.getElementById('docsReadPanel')!.style.display = 'none';
          document.getElementById('docsListContainer')!.style.display = 'block';
        }

        document.getElementById('docsClosePanel')?.addEventListener('click', closePanelIfOpen);

        document.getElementById('docsPanelCopyBtn')?.addEventListener('click', () => {
          navigator.clipboard.writeText(currentDocContent).then(() => {
            const btn = document.getElementById('docsPanelCopyBtn')!;
            btn.textContent = '✓ Copié';
            setTimeout(() => { btn.textContent = 'Copier'; }, 2000);
          });
        });

        // New folder
        document.getElementById('docsNewFolderBtn')?.addEventListener('click', () => {
          document.getElementById('docsNewFolderForm')!.style.display = 'flex';
          (document.getElementById('docsNewFolderInput') as HTMLInputElement).focus();
        });

        document.getElementById('docsNewFolderCancel')?.addEventListener('click', () => {
          document.getElementById('docsNewFolderForm')!.style.display = 'none';
          (document.getElementById('docsNewFolderInput') as HTMLInputElement).value = '';
        });

        document.getElementById('docsNewFolderSave')?.addEventListener('click', () => {
          const input = document.getElementById('docsNewFolderInput') as HTMLInputElement;
          const name = input.value.trim();
          if (!name) return;
          if (!allDossiers.includes(name)) {
            allDossiers.push(name);
            currentDossier = name;
            renderFolders();
            loadDocuments(name);
          }
          document.getElementById('docsNewFolderForm')!.style.display = 'none';
          input.value = '';
        });

        // Delete modal
        document.getElementById('docsDeleteConfirm')?.addEventListener('click', async () => {
          if (!pendingDeleteId) return;
          const modal = document.getElementById('docsDeleteModal') as HTMLElement;
          modal.style.display = 'none';
          try {
            await fetch('/api/documents', {
              method: 'DELETE',
              headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` },
              body: JSON.stringify({ id: pendingDeleteId })
            });
            loadDocuments(currentDossier!);
          } catch { /* noop */ }
          pendingDeleteId = null;
        });

        document.getElementById('docsDeleteCancel')?.addEventListener('click', () => {
          (document.getElementById('docsDeleteModal') as HTMLElement).style.display = 'none';
          pendingDeleteId = null;
        });

        // Init
        loadDossiers();
      }

      document.getElementById('logoutBtn')?.addEventListener('click', async () => {
        await supabase.auth.signOut();
        window.location.href = '/login';
      });

      document.getElementById('exportDataBtn')?.addEventListener('click', async () => {
        try {
          const response = await fetch('/api/export-data', {
            headers: { Authorization: `Bearer ${token}` }
          });

          if (!response.ok) {
            alert("Erreur lors de l'export. Veuillez réessayer.");
            return;
          }

          const blob = await response.blob();
          const url = URL.createObjectURL(blob);
          const anchor = document.createElement('a');
          anchor.href = url;
          anchor.download = 'maryan-mes-donnees.json';
          document.body.appendChild(anchor);
          anchor.click();
          anchor.remove();
          URL.revokeObjectURL(url);
        } catch {
          alert("Erreur lors de l'export. Veuillez réessayer.");
        }
      });
    }
  }
</script>

<style>
  .dashboard-page {
    background: var(--bg);
    min-height: 100vh;
    padding: 3rem 0 5rem;
  }

  .dashboard-layout {
    display: grid;
    grid-template-columns: minmax(0, 320px) minmax(0, 1fr);
    gap: 1.5rem;
    align-items: start;
  }

  .dashboard-sidebar {
    position: sticky;
    top: 1.5rem;
  }

  .dashboard-account {
    padding: 1.5rem;
    border-radius: 24px;
  }

  .dashboard-account:hover,
  .dashboard-hero:hover,
  .dashboard-resume-card:hover {
    transform: none;
    box-shadow: none;
    border-color: var(--border);
  }

  .dashboard-account h2 {
    font-size: 1.35rem;
    color: var(--deep);
    margin-bottom: 0.35rem;
  }

  .dashboard-account-meta {
    margin: 0;
    color: #45545F;
    font-size: 0.95rem;
    line-height: 1.5;
  }

  .dashboard-account-stats {
    display: grid;
    gap: 0.85rem;
    margin: 1.4rem 0;
  }

  .dashboard-account-stat {
    display: grid;
    gap: 0.2rem;
  }

  .dashboard-account-stat span,
  .dashboard-resume-label {
    font-size: 0.74rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #55646F;
  }

  .dashboard-account-stat strong {
    color: var(--deep);
    font-size: 0.96rem;
    font-weight: 700;
    word-break: break-word;
  }

  .dashboard-account-actions {
    display: grid;
    gap: 0.75rem;
  }

  .dashboard-profile-form-wrap {
    margin-top: 1.25rem;
    padding-top: 1rem;
    border-top: 1px solid var(--border);
  }

  .dashboard-profile-form-wrap h3 {
    margin: 0 0 0.35rem;
    font-size: 1.02rem;
    color: var(--deep);
  }

  .dashboard-profile-form-wrap > p {
    margin: 0 0 0.8rem;
    color: #55646F;
    font-size: 0.86rem;
    line-height: 1.45;
  }

  .dashboard-profile-form {
    display: grid;
    gap: 0.65rem;
  }

  .dashboard-profile-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.55rem;
  }

  .dashboard-profile-form label {
    display: grid;
    gap: 0.32rem;
  }

  .dashboard-profile-form label > span {
    font-size: 0.76rem;
    font-weight: 700;
    color: #4D5C66;
  }

  .dashboard-profile-form input,
  .dashboard-profile-form select {
    border: 1px solid var(--border);
    border-radius: 10px;
    min-height: 40px;
    padding: 0.5rem 0.65rem;
    font: inherit;
    color: var(--deep);
    background: white;
  }

  .dashboard-checkbox-row {
    grid-template-columns: 16px 1fr;
    align-items: center;
    gap: 0.5rem;
    margin-top: 0.2rem;
  }

  .dashboard-checkbox-row input {
    min-height: auto;
    width: 16px;
    height: 16px;
    margin: 0;
  }

  .dashboard-profile-feedback {
    border-radius: 10px;
    padding: 0.55rem 0.7rem;
    font-size: 0.82rem;
    font-weight: 700;
  }

  .dashboard-profile-feedback--ok {
    background: #ECFDF3;
    color: #1B5E3B;
    border: 1px solid #BBE7CD;
  }

  .dashboard-profile-feedback--error {
    background: #FFF1F2;
    color: #8A1C2B;
    border: 1px solid #F8C6CE;
  }

  .dashboard-account-actions .btn-secondary-v3 {
    width: 100%;
    justify-content: center;
    text-align: center;
  }

  .dashboard-logout-btn {
    border: none;
    background: transparent;
    padding: 0.2rem 0;
    color: var(--accent-red);
    font-size: 0.92rem;
    font-weight: 800;
    font-family: inherit;
    cursor: pointer;
    text-align: left;
  }

  .dashboard-main {
    display: grid;
    gap: 1.25rem;
  }

  .dashboard-banner {
    background: #F0FFF4;
    border: 1px solid #B7E4C7;
    border-radius: 18px;
    padding: 1rem 1.2rem;
  }

  .dashboard-banner strong {
    display: block;
    color: #1F5F3E;
    margin-bottom: 0.2rem;
  }

  .dashboard-banner p {
    margin: 0;
    color: #2C5D46;
    font-size: 0.92rem;
  }

  .dashboard-banner-upgrade {
    background: #FFFDF2;
    border-color: #E8E0A5;
    display: grid;
    gap: 0.75rem;
    align-items: start;
  }

  .dashboard-hero,
  .dashboard-block,
  .dashboard-resume-card {
    border-radius: 24px;
  }

  .dashboard-hero {
    padding: 1.5rem;
  }

  .dashboard-hero h1 {
    font-size: clamp(2rem, 5vw, 2.8rem);
    color: var(--deep);
    margin-bottom: 0.5rem;
  }

  .dashboard-hero-copy {
    margin: 0;
    color: #41505B;
    font-size: 1rem;
    line-height: 1.6;
    max-width: 48rem;
  }

  .dashboard-block {
    background: white;
    border: 1px solid var(--border);
    padding: 1.5rem;
  }

  .dashboard-block-head {
    display: flex;
    justify-content: space-between;
    align-items: end;
    gap: 1rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }

  .dashboard-head-cta {
    font-size: 0.9rem;
    padding: 0.6rem 1rem;
    border-radius: 12px;
    text-decoration: none;
  }

  .dashboard-block-head h2 {
    font-size: 1.45rem;
    color: var(--deep);
    margin-bottom: 0.2rem;
  }

  .dashboard-block-head p {
    margin: 0;
    color: #52616B;
    font-size: 0.95rem;
  }

  .dashboard-action-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.85rem;
  }

  .dashboard-action-card {
    display: flex;
    align-items: center;
    min-height: 84px;
    border-radius: 18px;
    border: 1px solid var(--border);
    background: #FCFCFB;
    padding: 1rem 1.1rem;
    color: var(--deep);
    text-decoration: none;
    font-size: 1rem;
    font-weight: 800;
    line-height: 1.35;
    transition: border-color 0.15s ease, transform 0.15s ease;
  }

  .dashboard-action-card:hover,
  .dashboard-action-card:focus-visible {
    border-color: var(--deep);
    outline: none;
    transform: translateY(-1px);
  }

  .dashboard-support-row {
    display: flex;
    gap: 0.75rem;
    align-items: center;
    flex-wrap: wrap;
    margin-top: 1rem;
  }

  .dashboard-support-badge {
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    padding: 0.35rem 0.75rem;
    background: rgba(173,255,47,0.16);
    color: var(--deep);
    font-size: 0.76rem;
    font-weight: 800;
  }

  .dashboard-support-badge--locked {
    background: rgba(230,57,70,0.12);
    color: var(--accent-red);
  }

  .dashboard-support-link {
    color: var(--accent-red);
    font-weight: 800;
    text-decoration: none;
    border-bottom: 1px solid rgba(230,57,70,0.25);
  }

  .dashboard-support-link:hover,
  .dashboard-support-link:focus-visible {
    border-bottom-color: var(--accent-red);
    outline: none;
  }

  .dashboard-logout-btn:focus-visible,
  .dashboard-account-actions .btn-secondary-v3:focus-visible {
    outline: 2px solid var(--accent-red);
    outline-offset: 2px;
  }

  .dashboard-radar-list {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.85rem;
  }

  .dashboard-radar-card {
    border-radius: 18px;
    border: 1px solid var(--border);
    background: #FCFCFB;
    padding: 1rem;
  }

  .dashboard-radar-top {
    display: flex;
    justify-content: space-between;
    gap: 0.75rem;
    align-items: center;
    margin-bottom: 0.75rem;
  }

  .dashboard-radar-badge {
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    padding: 0.3rem 0.65rem;
    background: var(--deep);
    color: white;
    font-size: 0.68rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .dashboard-radar-date {
    color: #596874;
    font-size: 0.8rem;
    font-weight: 700;
  }

  .dashboard-radar-card h3 {
    color: var(--deep);
    font-size: 1rem;
    line-height: 1.3;
    margin-bottom: 0.7rem;
  }

  .dashboard-radar-card p {
    margin: 0;
    color: #42515C;
    font-size: 0.9rem;
    line-height: 1.55;
  }

  .dashboard-radar-card p + p {
    margin-top: 0.5rem;
  }

  .dashboard-topics-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.85rem;
  }

  .dashboard-topic-card {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: space-between;
    min-height: 88px;
    border-radius: 18px;
    border: 1px solid var(--border);
    background: #FCFCFB;
    padding: 0.95rem 1rem;
    color: var(--deep);
    text-decoration: none;
    font-size: 0.96rem;
    font-weight: 700;
    line-height: 1.45;
  }

  .dashboard-topic-title {
    display: block;
  }

  .dashboard-topic-link {
    margin-top: 0.45rem;
    font-size: 0.82rem;
    font-weight: 800;
    color: var(--accent-red);
  }

  .dashboard-topic-card:hover,
  .dashboard-topic-card:focus-visible {
    border-color: var(--border-neon);
    outline: none;
  }

  .dashboard-resume-card {
    padding: 1.5rem;
  }

  .dashboard-resume-live h3,
  .dashboard-resume-empty h3 {
    color: var(--deep);
    font-size: 1.2rem;
    margin-bottom: 0.4rem;
  }

  .dashboard-resume-live p,
  .dashboard-resume-empty p {
    margin: 0 0 1rem;
    color: #45545F;
    font-size: 0.95rem;
    line-height: 1.55;
  }

  @media (max-width: 1080px) {
    .dashboard-layout {
      grid-template-columns: 1fr;
    }

    .dashboard-sidebar {
      position: static;
    }

    .dashboard-radar-list {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 720px) {
    .dashboard-page {
      padding: 2rem 0 4rem;
    }

    .dashboard-account,
    .dashboard-hero,
    .dashboard-block,
    .dashboard-resume-card {
      padding: 1.2rem;
      border-radius: 20px;
    }

    .dashboard-action-grid,
    .dashboard-topics-grid {
      grid-template-columns: 1fr;
    }

    .dashboard-profile-grid {
      grid-template-columns: 1fr;
    }

    .dashboard-action-card,
    .dashboard-topic-card {
      min-height: auto;
    }
  }

  @media print {
    .dashboard-sidebar,
    .dashboard-support-row,
    .btn-link {
      display: none !important;
    }

    .dashboard-layout {
      grid-template-columns: 1fr;
    }

    .dashboard-page {
      background: white;
      padding: 0;
    }
  }

  /* ── MES DOCUMENTS ── */
  .docs-folders-bar {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 1rem;
  }

  .docs-folders-list {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    flex: 1;
  }

  .docs-folder-btn {
    appearance: none;
    border: 1.5px solid rgba(10, 25, 47, 0.1);
    background: transparent;
    border-radius: 999px;
    padding: 0.4rem 0.9rem;
    font: inherit;
    font-size: 0.85rem;
    font-weight: 700;
    color: rgba(10, 25, 47, 0.6);
    cursor: pointer;
    transition: all 0.15s;
  }

  .docs-folder-btn:hover { border-color: var(--deep); color: var(--deep); }

  .docs-folder-btn--active {
    background: var(--deep);
    border-color: var(--deep);
    color: #fff;
  }

  .docs-new-folder-btn {
    appearance: none;
    border: 1.5px dashed rgba(10, 25, 47, 0.2);
    background: transparent;
    border-radius: 999px;
    padding: 0.4rem 0.85rem;
    font: inherit;
    font-size: 0.82rem;
    font-weight: 700;
    color: rgba(10, 25, 47, 0.45);
    cursor: pointer;
    transition: all 0.15s;
  }

  .docs-new-folder-btn:hover { border-color: var(--deep); color: var(--deep); }

  .docs-folder-chip { display: inline-flex; }

  .docs-folder-rename-input {
    border: 1.5px solid var(--deep);
    border-radius: 999px;
    padding: 0.4rem 0.85rem;
    font: inherit;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--deep);
    outline: none;
    min-width: 80px;
    max-width: 180px;
  }

  .docs-new-folder-form {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 0.75rem;
  }

  .docs-folder-input {
    border: 1.5px solid rgba(10, 25, 47, 0.15);
    border-radius: 8px;
    padding: 0.5rem 0.85rem;
    font: inherit;
    font-size: 0.9rem;
    color: var(--deep);
    outline: none;
    flex: 1;
    min-width: 160px;
  }

  .docs-folder-input:focus { border-color: var(--deep); }

  .btn-mode-sm-primary {
    background: var(--deep);
    color: #fff;
    border: none;
    border-radius: 8px;
    padding: 0.5rem 1rem;
    font: inherit;
    font-size: 0.85rem;
    font-weight: 700;
    cursor: pointer;
  }

  .btn-mode-sm-primary:hover { background: #0d2140; }

  .btn-mode-sm-secondary {
    background: transparent;
    border: 1.5px solid rgba(10, 25, 47, 0.15);
    color: var(--deep);
    border-radius: 8px;
    padding: 0.5rem 1rem;
    font: inherit;
    font-size: 0.85rem;
    font-weight: 700;
    cursor: pointer;
  }

  .btn-mode-sm-secondary:hover { border-color: var(--deep); }

  .btn-mode-sm-outline {
    background: transparent;
    border: none;
    color: rgba(10, 25, 47, 0.5);
    padding: 0.5rem 0.75rem;
    font: inherit;
    font-size: 0.85rem;
    font-weight: 700;
    cursor: pointer;
    text-decoration: underline;
  }

  .btn-mode-sm-danger {
    background: var(--accent-red);
    color: #fff;
    border: none;
    border-radius: 8px;
    padding: 0.5rem 1.1rem;
    font: inherit;
    font-size: 0.88rem;
    font-weight: 700;
    cursor: pointer;
  }

  .docs-list-container { position: relative; }

  .docs-loading {
    color: rgba(10, 25, 47, 0.5);
    font-size: 0.92rem;
    padding: 0.75rem 0;
  }

  .docs-empty {
    padding: 1.5rem 0;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
    align-items: flex-start;
  }

  .docs-empty p { margin: 0; color: #54636C; font-size: 0.95rem; }

  .docs-list {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .docs-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.85rem 1rem;
    border-radius: 14px;
    border: 1px solid rgba(10, 25, 47, 0.08);
    background: #FAFAF8;
    flex-wrap: wrap;
  }

  .docs-item-main {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex: 1;
    min-width: 0;
  }

  .docs-item-icon { font-size: 1.2rem; flex-shrink: 0; }

  .docs-item-info {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    min-width: 0;
  }

  .docs-item-title {
    font-size: 0.94rem;
    font-weight: 700;
    color: var(--deep);
    cursor: pointer;
    text-decoration: underline;
    text-decoration-color: rgba(10, 25, 47, 0.2);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .docs-item-title:hover { text-decoration-color: var(--deep); }

  .docs-item-meta {
    font-size: 0.78rem;
    color: rgba(10, 25, 47, 0.45);
    font-weight: 600;
  }

  .docs-item-actions {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  .docs-move-select {
    border: 1px solid rgba(10, 25, 47, 0.12);
    border-radius: 6px;
    padding: 0.3rem 0.6rem;
    font: inherit;
    font-size: 0.8rem;
    color: rgba(10, 25, 47, 0.55);
    background: white;
    cursor: pointer;
    outline: none;
    max-width: 130px;
  }

  .docs-delete-btn {
    appearance: none;
    border: none;
    background: transparent;
    cursor: pointer;
    font-size: 1rem;
    color: rgba(10, 25, 47, 0.35);
    padding: 0.25rem;
    transition: color 0.15s;
  }

  .docs-delete-btn:hover { color: var(--accent-red); }

  /* ── Panneau de lecture ── */
  .docs-read-panel {
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .docs-read-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .docs-close-panel-btn {
    appearance: none;
    border: none;
    background: transparent;
    color: rgba(10, 25, 47, 0.55);
    font: inherit;
    font-size: 0.85rem;
    font-weight: 700;
    cursor: pointer;
    padding: 0;
    text-decoration: underline;
  }

  .docs-close-panel-btn:hover { color: var(--deep); }

  .docs-read-meta {
    display: flex;
    gap: 0.65rem;
    align-items: center;
    flex-wrap: wrap;
  }

  .docs-type-badge {
    font-size: 0.78rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: rgba(10, 25, 47, 0.5);
    background: rgba(10, 25, 47, 0.06);
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
  }

  .docs-date { font-size: 0.82rem; color: rgba(10, 25, 47, 0.45); font-weight: 600; }

  .docs-read-title {
    font-size: 1.25rem;
    font-weight: 800;
    color: var(--deep);
    margin: 0;
    line-height: 1.2;
  }

  .docs-read-content {
    background: #FAFAF8;
    border-radius: 14px;
    padding: 1.25rem 1.4rem;
    border: 1px solid rgba(10, 25, 47, 0.07);
    line-height: 1.65;
    color: var(--ink);
  }

  .docs-read-content .doc-md-h2 {
    font-size: 1rem;
    font-weight: 800;
    color: var(--deep);
    margin: 1rem 0 0.4rem;
    padding-bottom: 0.3rem;
    border-bottom: 1px solid rgba(10, 25, 47, 0.08);
  }

  .docs-read-content .doc-md-h3 {
    font-size: 0.92rem;
    font-weight: 800;
    color: var(--deep);
    margin: 0.9rem 0 0.35rem;
  }

  .docs-read-content .doc-md-p {
    margin: 0.35rem 0;
    font-size: 0.94rem;
  }

  .docs-read-content .doc-md-list {
    margin: 0.35rem 0 0.35rem 1.2rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .docs-read-content .doc-md-list li { font-size: 0.93rem; line-height: 1.5; }

  /* ── Modal suppression ── */
  .docs-modal-overlay {
    position: fixed;
    inset: 0;
    z-index: 100;
    background: rgba(10, 25, 47, 0.35);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
  }

  .docs-modal {
    background: white;
    border-radius: 18px;
    padding: 2rem;
    max-width: 380px;
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    box-shadow: 0 24px 60px rgba(10, 25, 47, 0.18);
  }

  .docs-modal-title {
    margin: 0;
    font-size: 1.1rem;
    font-weight: 800;
    color: var(--deep);
  }

  .docs-modal-text { margin: 0; color: #54636C; font-size: 0.95rem; }

  .docs-modal-actions {
    display: flex;
    gap: 0.75rem;
    margin-top: 0.25rem;
    flex-wrap: wrap;
  }

  @media (max-width: 720px) {
    .docs-item { gap: 0.65rem; }
    .docs-item-actions { flex-wrap: wrap; }
    .docs-move-select { max-width: 100px; }
  }
</style>
